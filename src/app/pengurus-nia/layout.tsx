"use client";
import { NiaShell } from "@/components/layout/nia-shell";

export default function PengurusNiaLayout({ children }: { children: React.ReactNode }) {
  return <NiaShell>{children}</NiaShell>;
}
